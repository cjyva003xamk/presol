import React, { useRef, useState } from 'react';
import { Slider, Alert, Card, CardMedia, Button, Container, FormControl, List, ListItem, ListItemText, FormLabel, FormControlLabel, RadioGroup, Grid, Radio, Typography, Paper, Stack, TextField } from '@mui/material';
import { ArrowDropDown, ArrowDropUp } from "@mui/icons-material";

const Landing: React.FC = (): React.ReactElement => {

    return (
        <Container>
            <Typography><h4>Olet etusivulla</h4></Typography>

        </Container>
    );
}

export default Landing;
