import React, { useRef, useState } from 'react';
import {  Container, Typography } from '@mui/material';

const Landing: React.FC = (): React.ReactElement => {

    return (
        <Container>
            <Typography><h4>Olet etusivulla</h4></Typography>

        </Container>
    );
}

export default Landing;
